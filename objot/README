== Objot version 23 ==

Objot is a lightweight service oriented library for efficient web development,
 based on Java 6, Javascript, Actionscript 3 etc.

Supports Firefox 3, Firefox 2, IE 7, IE 6, Opera 9.5 and Safari 3.1 and Adobe 
 Flash Player 9

Improvements
  * read Java 8 bytecode but still generate Java 5 bytecode
  * fix: codec format error if Map key is }
  * fix: integer overflow of binary search
  * minor fixes
  * minor improvements
  * built on at least Java 6

Todo
  * Javascript codec supporting a kind of Lua codec format

== Objot 版本 23 ==

Objot是一个面向服务的轻量级web开发库，可以根据需要和其他框架组合。
服务器部署业务数据，部署业务逻辑Service；浏览器及Flash部署视图，部署操作逻辑，与Service交互。
交互的数据、流程由开发者定义，用Objot实现。

Objot支持Java 6、Javascript、Actionscript 3，
支持Firefox 3、Firefox 2、IE 7、IE 6、Opera 9.5、Safari 3.1、Adobe Flash Player
 9。

包含一个在线聊天的范例。

改进：
  * 读取Java 8字节码，生成Java 5字节码
  * 修复Map key为}时编码格式的错误
  * 修复二分查找中的整数溢出错误
  * 错误修复
  * 细节改进
  * Java 6以上编译

后续改进：
  * 支持一种Lua编码格式的编码器


== Objot version 22 ==

Objot is a lightweight service oriented library for efficient web development,
 based on Java 5, Javascript, Actionscript 3 etc.

Supports Firefox 3, Firefox 2, IE 7, IE 6, Opera 9.5 and Safari 3.1 and Adobe 
 Flash Player 9

Improvements
  * an optional fast binary codec format for Java only
  * Codec.name and Codec.byName support rule key
  * Codec.newList and Codec.undecodable
  * $enc.ref and $enc.unref support rule key
  * bug fixes
  * minor improvements


== Objot 版本 22 ==

Objot是一个面向服务的轻量级web开发库，可以根据需要和其他框架组合。
服务器部署业务数据，部署业务逻辑Service；浏览器及Flash部署视图，部署操作逻辑，与Service交互。
交互的数据、流程由开发者定义，用Objot实现。

Objot支持Java 5、Javascript、Actionscript 3，
支持Firefox 3、Firefox 2、IE 7、IE 6、Opera 9.5、Safari 3.1、Adobe Flash Player
 9。

包含一个在线聊天的范例。

改进：
  * 可选的针对Java的快速二进制编码格式
  * Codec.name和Codec.byName支持编码规则
  * Codec.newList和Codec.undecodable
  * $enc.ref和$enc.unref支持编码规则
  * 错误修复
  * 细节改进


== Objot version 21 ==

Objot is a lightweight service oriented library for efficient web development,
 based on Java 5, Javascript, Actionscript 3 etc.

Supports Firefox 3, Firefox 2, IE 7, IE 6, Opera 9.5 and Safari 3.1 and Adobe 
 Flash Player 9

Improvements
  * bytecode parser and generator
  * faster codec
  * configure codec rules by user codes
  * codec on Actionscript 3
  * encode and decode single primitive data
  * ultralight, very fast and powerful IOC container
  * ultralight, very fast and powerful AOP implementation
  * file upload
  * upload progress notification
  * multi service parameters
  * documents
  * many bug fixes
  * many minor improvements
  * change to the GNU Lesser General Public License version 2.1

Todo
  * IOC: provider mode
  * AOP: inherit all annotations from target methods


== Objot 版本 21 ==

Objot是一个面向服务的轻量级web开发库，可以根据需要和其他框架组合。
服务器部署业务数据，部署业务逻辑Service；浏览器及Flash部署视图，部署操作逻辑，与Service交互。
交互的数据、流程由开发者定义，用Objot实现。

Objot支持Java 5、Javascript、Actionscript 3，
支持Firefox 3、Firefox 2、IE 7、IE 6、Opera 9.5、Safari 3.1、Adobe Flash Player
 9。

包含一个在线聊天的范例。

改进：
  * 字节码解析和生成
  * 更快的编解码器
  * 代码式编解码配置
  * 编解码支持Actionscript 3
  * 编解码单个原始类型
  * 轻量、快速而强大的IOC容器
  * 轻量、快速而强大的AOP实现
  * 文件上传
  * 上传进度提示
  * 支持多个参数的服务
  * 文档更新
  * 错误修复
  * 许多细节改进
  * 采用GNU LGPL版本2.1许可模式

后续改进：
  * IOC: 支持provider模式
  * AOP: 继承target方法的全部annotation
